---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Breadcrumb from '../../components/Breadcrumb.astro';
import Rubric from '../../components/Rubric.astro';
import EmptyState from '../../components/EmptyState.astro';
import EntryList from '../../components/EntryList.astro';

const entries = await getCollection('projects', (e) =>
  !e.data.draft && (e.data.locale ?? 'pt') === 'pt'
);
entries.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());

const listEntries = entries.map((e) => ({
  href: `/opera/${e.slug}/`,
  title: e.data.title,
  date: e.data.date,
  description: e.data.description,
  meta: e.data.stack,
}));
---
<BaseLayout
  title="Opera · S3r4ph1el"
  description="Código, ferramentas e experimentos publicados — S3r4ph1el."
>
  <div class="terminal-wrap narrow">
    <Breadcrumb trail={[
      { label: 'Index', href: '/' },
      { label: 'Opera' },
    ]} />

    <Rubric
      numeral="II"
      title="Opera"
      subtitle="Código, ferramentas e experimentos."
    />

    {listEntries.length === 0 ? (
      <EmptyState
        latin="Nondum opera."
        vern="Ainda não há obras publicadas."
      />
    ) : (
      <EntryList entries={listEntries} lang="pt" />
    )}
  </div>
</BaseLayout>

<style>
  .narrow { max-width: 880px; }
</style>
